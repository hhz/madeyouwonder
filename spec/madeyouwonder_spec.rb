require "spec_helper"

describe Madeyouwonder do
  it "has a version number" do
    expect(Madeyouwonder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
